// examples/demo.ltl — runnable demo for ltl_delaunay

import std::io
use ltl_delaunay as lib

fn main() {
    io::println("ltl_delaunay demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}